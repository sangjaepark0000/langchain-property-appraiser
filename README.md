# langchain-property-appraiser

Local-first RAG learning project for property-appraisal related document workflows.

## Backend

The backend is implemented first using FastAPI. See [`backend/README.md`](backend/README.md) for environment setup, dependency installation, local API startup, and tests.
